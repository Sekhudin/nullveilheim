{ ... }:

{
  i18n = {
    defaultLocale = "en_US.UTF-8";
  };

  time = {
    timeZone = "Asia/Jakarta";
  };

  console = {
    keyMap = "us";
  };

  security = {
    polkit = {
      enable = true;
    };
  };
}
